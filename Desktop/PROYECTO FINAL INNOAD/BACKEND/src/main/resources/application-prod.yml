# Configuración para entorno de producción (Azure PostgreSQL)
spring:
  datasource:
    # Usa las variables de Azure PostgreSQL (o variables locales DB_*)
    url: jdbc:postgresql://${DB_HOST:${AZURE_DB_HOST:innoad-postgres.postgres.database.azure.com}}:${DB_PORT:${AZURE_DB_PORT:5432}}/${DB_NAME:${AZURE_DB_NAME:innoad_db}}?sslmode=require&connectTimeout=30&socketTimeout=0&tcpKeepAlive=true
    username: ${DB_USER:${AZURE_DB_USER:admininnoad}}
    password: ${DB_PASSWORD:${AZURE_DB_PASSWORD:}}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 60000
      maximum-pool-size: 5
      minimum-idle: 2
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 1
      leak-detection-threshold: 60000
  
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          batch_size: 20
          time_zone: UTC
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false
  
  # Configuración de correo (usar variables de entorno)
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
  
  # Redis para producción y Docker
  data:
    redis:
        host: ${REDIS_HOST:redis.internal}
        port: ${REDIS_PORT:6379}
        password: ${REDIS_PASSWORD:}
        timeout: 60000ms

# Logging optimizado para producción
logging:
  level:
    root: WARN
    com.innoad: INFO
    org.springframework.web: WARN
    org.hibernate: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# Actuator para health checks
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized
  health:
    mail:
      enabled: false
    redis:
      enabled: false

# Server configuration
server:
  port: ${PORT:80}
  compression:
    enabled: true
  error:
    include-message: always
    include-stacktrace: never

# Configuración de Mercado Pago - Producción
mercado-pago:
  access-token: ${MERCADO_PAGO_ACCESS_TOKEN:}
  public-key: ${MERCADO_PAGO_PUBLIC_KEY:}
  private-key: ${MERCADO_PAGO_PRIVATE_KEY:}
  webhook-secret: ${MERCADO_PAGO_WEBHOOK_SECRET:}
  webhook-url: ${MERCADO_PAGO_WEBHOOK_URL:}
  client-id: ${MERCADO_PAGO_CLIENT_ID:}
  client-secret: ${MERCADO_PAGO_CLIENT_SECRET:}

# Configuración de aplicación
app:
  name: InnoAd Signage Platform
  version: 1.0.0
  environment: PRODUCCION
  max-file-size: 104857600 # 100MB
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://innoad.com,https://www.innoad.com}
    allowed-methods: GET,POST,PUT,DELETE,OPTIONS
    allowed-headers: "*"
    allow-credentials: true
    max-age: 3600
