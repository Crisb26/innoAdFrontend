# ============================================================================
# CONFIGURACIÓN SERVIDOR CASERO - INNOAD
# ============================================================================
# INSTRUCCIONES:
#   1. Copiar este archivo a .env.server
#   2. Rellenar TODOS los valores marcados con CAMBIAR_
#   3. NUNCA comitear .env.server a Git
# ============================================================================

# ==================== RED PRIVADA ====================
# Tu IP de servidor casero (ej: 192.168.1.100 o 10.0.0.50)
SERVER_IP=192.168.1.XXX
DOCKER_HOST_IP=${SERVER_IP}

# ==================== BASE DE DATOS PostgreSQL ====================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=innoad_db
DB_USERNAME=postgres
DB_PASSWORD=CAMBIAR_password_segura_de_postgres
POSTGRES_INITDB_ARGS=-c shared_buffers=256MB -c effective_cache_size=1GB

# ==================== SPRING BOOT ====================
SPRING_PROFILES_ACTIVE=server
SPRING_APPLICATION_NAME=innoad-backend
SERVER_PORT=8080
SERVER_SERVLET_CONTEXT_PATH=/api

# ==================== CORS - Network Privada ====================
# Permite conexiones desde tu red casera (ajusta según tu rango IP)
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:3000,http://localhost:4200,http://${SERVER_IP},http://${SERVER_IP}:3000,http://${SERVER_IP}:4200,http://192.168.1.*,http://10.0.0.*

# ==================== SEGURIDAD JWT ====================
# ⚠️  CRÍTICO: Generar un valor seguro con el siguiente comando:
#     Windows PowerShell: [Convert]::ToBase64String((1..64 | %{Get-Random -Maximum 256}))
#     Linux/Mac/Git Bash: openssl rand -base64 64
#
# Ejemplo de formato correcto (NO USAR ESTE, generar el tuyo):
# JWT_SECRET=YrEdXi+kcKSzb+3gllte6npGnxLZELcqsZkmHt4aJXxOu+QKnw79uRneGywAoAITmKHJHl9EVgNzRZhq/aOyJw==
JWT_SECRET=CAMBIAR_ESTE_SECRETO_POR_UNO_GENERADO_CON_OPENSSL_RAND_BASE64_64
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# ==================== FRONTEND URLs ====================
VITE_API_URL=http://${SERVER_IP}:8080/api
ANGULAR_API_URL=http://${SERVER_IP}:8080/api

# ==================== EMAIL (Opcional - Gmail con App Password) ====================
# Para generar App Password: https://support.google.com/accounts/answer/185833
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=CAMBIAR_tu_email@gmail.com
MAIL_PASSWORD=CAMBIAR_tu_app_password_16_caracteres

# ==================== OPENAI / IA (Opcional) ====================
# Dejar vacío si no usas el módulo de chat IA
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4-turbo

# ==================== LOGGING ====================
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_INNOAD=INFO
LOGGING_FILE_NAME=logs/innoad.log
LOGGING_FILE_MAX_SIZE=10MB

# ==================== DESHABILITADAS EN PRODUCCIÓN ====================
H2_CONSOLE_ENABLED=false
SWAGGER_ENABLED=false

# ==================== MANTENIMIENTO ====================
# Código para activar/desactivar modo mantenimiento
MAINTENANCE_CODE=93022611184
